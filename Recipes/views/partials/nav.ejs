<div class="menu-bar">
    <nav>
        <% if (user) { %>
            <a href="/logout">Log Out</a>
        <% } else { %>
            <a href="/auth/google">Login</a>
        <% } %>
        <a href="/recipes" <%- title === 'All Recipes' ? 'class="active"' : '' %>>BROWSE</a>
    </nav>
</div>